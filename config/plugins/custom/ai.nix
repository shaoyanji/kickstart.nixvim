{
  # Supermaven currently aborts startup when its external binary metadata is blank
  # or otherwise unavailable. Keep AI config non-fatal by disabling it entirely
  # until it can be reintroduced with a safe initialization path.
}
